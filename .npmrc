# next-auth@4 does not yet list Next.js 16 as a supported peer dependency.
# Until we migrate to Auth.js v5, allow installs to proceed past the peer conflict.
# This keeps CI/Vercel `npm install` consistent with local installs.
legacy-peer-deps=true
